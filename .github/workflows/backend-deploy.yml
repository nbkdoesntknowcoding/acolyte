name: Backend Deploy
on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  deploy-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: cd backend && flyctl deploy --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  # TODO: Enable when worker/beat Fly apps are created
  # deploy-worker:
  #   needs: deploy-api
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: superfly/flyctl-actions/setup-flyctl@master
  #     - run: cd backend && flyctl deploy --remote-only --app acolyte-worker
  #       env:
  #         FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
  #
  # deploy-beat:
  #   needs: deploy-api
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: superfly/flyctl-actions/setup-flyctl@master
  #     - run: cd backend && flyctl deploy --remote-only --app acolyte-beat
  #       env:
  #         FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
